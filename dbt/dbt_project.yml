name: "treasury_intelligence"
version: "1.0.0"
config-version: 2

profile: "treasury_intelligence"

model-paths:
  - "models"

clean-targets:
  - "target"
  - "dbt_packages"

models:
  treasury_intelligence:
    staging:
      +materialized: view
      +schema: staging
